export const settingFlag = 'setting';
export const sidebarSettingFlag = 'sidebar';
export const localeFlag = 'locale';

const settingRepository = {
  settingFlag,
  sidebarSettingFlag,
  localeFlag,
};

export default settingRepository;
